1. binding
2. type-checked
3. static environment
4. dynamic env = env
5. context = static env
6. read-eval-print loop
7. interpreter
8. semantic (how it type=checks and evaluates)
9. what is a value?
10. what is construct? what construct means in "Whenever you learn a new construct in a programming language, you should ask these three
    questions: What is the syntax? What are the type-checking rules? What are the evaluation
    rules?"
11. shadow?
    `You can have another binding later, say val x = 19;,
but that just creates a different environment where the later binding for x shadows the earlier one. This
distinction will be extremely important when we define functions that use variables`

12.function bindings 13. pairs and lists 14. what're the different between function and methods, compare sml vs Java 15.
